import { locales } from "./constants";
import { EN_CONTENT_HOME } from "./en";
import { ES_CONTENT_HOME } from "./es";
import { PT_CONTENT_HOME } from "./pt";

const {ES, EN, PT} = locales

export const CONTEN_BY_LOACALES = {
  [ES]: ES_CONTENT_HOME,
  [EN]: EN_CONTENT_HOME,
  [PT]: PT_CONTENT_HOME,
}